const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao,multiplicacao, -divisao, modulo)

// Operador binário → tem dois operandos
// op1 + op2 // + → operador "infix": fica após o operando
// ++d → operador "prefix": fica antes do operando (operador binário)
// d++ → operador "postfix": fica após o operando (operador binário)
// -a → - (operador unário)