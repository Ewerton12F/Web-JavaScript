/*
* 03) Crie uma função que recebe dois parâmetros, base e expoente,
* e retorne a base elevada ao expoente.
*/

function expoente(base, expo) {
    console.log(`A base ${base} elevada ao expoente ${expo} é ${Math.pow(base, expo)}`)
} 

expoente(5, 2)